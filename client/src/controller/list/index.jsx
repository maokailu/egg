import React from 'react';
import ReactDom from 'react-dom';
class List extends React.Component {
    render() {
        return (
            <div>
              list
            </div>
        );
    }
}

export default List;

// ReactDom.hydrate(<List />, document.querySelector('body'));
